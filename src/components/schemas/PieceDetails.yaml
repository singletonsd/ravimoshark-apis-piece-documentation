description: Pieces detail information.
type: object
properties:
  id:
    type: integer
    format: int64
  piece:
    $ref: "#/components/schemas/Pieces"
  refArticle:
    type: string
  title:
    type: string
  content:
    type: string
  unit:
    type: string
  line:
    type: integer
    format: int64
  createdAt:
    type: string
    format: date
  updatedAt:
    type: string
    format: date
  deleted:
    type: boolean
